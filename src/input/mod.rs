/// The module containing advanced event to bind conversions
pub mod controller;
/// The main InputEvent Enum and keyboard event
pub mod event;
/// The manipulation of player's inputs from events
pub mod input;
/// The list of inputs a player can emit
pub mod input_name;
/// The states an input can be in during any tick
pub mod input_state;
/// Extra utilities to look at all of the players' inputs at once
pub mod input_vec;
