/// The singleton containing the path to the save file
pub mod path;
/// The single player save data
pub mod save;
/// The functions to save (Serialize) and load (Deserialize) the save file
pub mod settings;
